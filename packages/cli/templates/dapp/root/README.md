# {{PROJECT_NAME}}

A Midnight DApp built with [Compact](https://docs.midnight.network/) smart contracts.

## Structure

- `api/` — Contract API layer: providers, witnesses, and contract deployment
- `ui/` — React frontend with Vite

## Development

```bash
{{PACKAGE_MANAGER}} run dev
```

## Build

```bash
{{PACKAGE_MANAGER}} run build
```
