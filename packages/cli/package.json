{
  "name": "@shieldedtech/moth-cli",
  "version": "0.13.0",
  "description": "CLI tool for Midnight Network wallet operations and contract deployments. USE AT YOUR OWN RISK. Experimental software provided AS-IS with no warranty. Not audited. For development and testing purposes only.",
  "license": "Apache-2.0",
  "type": "module",
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist",
    "templates"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/shieldedtech/moth-wallet.git",
    "directory": "packages/cli"
  },
  "bin": {
    "moth": "./dist/index.js"
  },
  "exports": {
    ".": "./dist/index.js",
    "./adapters/*": "./dist/adapters/*.js"
  },
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc && chmod +x dist/index.js",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@oclif/core": "^4.2.0",
    "@shieldedtech/moth-tui": "^0.13.0",
    "@shieldedtech/moth-wallet": "^0.13.0",
    "dotenv": "17.4.2",
    "ink": "^7.0.1"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^5.5.0",
    "vitest": "^3.1.0"
  },
  "oclif": {
    "bin": "moth",
    "commands": {
      "strategy": "pattern",
      "target": "./dist/commands"
    },
    "topicSeparator": " ",
    "topics": {
      "maintenance": {
        "description": "Contract maintenance updates: verifier keys, and the authority that can change them"
      },
      "preseed": {
        "description": "Inspect and maintain this machine's pre-seed reference: one empty wallet synced to chain tip, which lets a new wallet start from its state instead of walking the chain."
      }
    }
  }
}
