{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".output/**", ".wxt/**"],
      "env": ["MOTH_EXT_OUT_DIR"]
    }
  }
}
